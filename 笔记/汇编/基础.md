#### 基础知识
汇编语言的组成：汇编指令，伪指令，其他符号
存储器：存贮数据和指令的地方，然后让cpu在存储器中进行读取，进行运算，内存
指令和数据在内存和磁盘中，都是二进制信息，只有在cpu中进行读取才能识别，分辨
存储单元：存储器分为若干的存储单元，每个存储单元从0开始，每个存储单元可以存储一个字节
cpu对存储器的读写：
cpu要从内存中读取数据，首先要指定存储单元的地址，cpu在读写数据时要指明，从哪个器件进行操作，进行什么操作，
cpu与芯片进行三类交互：存储单元的地址，器件的选择，读或者写的命令，读写的数据
